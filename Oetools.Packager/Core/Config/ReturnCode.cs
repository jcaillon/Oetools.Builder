namespace Oetools.Packager.Core.Config {
    public enum ReturnCode {
        NoSet,
        DeploymentError,
        CompilationError,
        CompilationErrorOnMaxUser,
        Ok,
        Canceled
    }
}